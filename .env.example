# Required
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional — override default model IDs in generated configs
# OPENAI_MODEL=gpt-4o
# CLAUDE_MODEL=claude-sonnet-4-20250514

# Optional — comma-separated CORS origins
# CORS_ORIGINS=https://internal.manageai.io,http://localhost:8080

# Railway injects this automatically
# PORT=8001
